services:
  app:
    build: sollo/polling-image:latest
    container_name: pollimg
    env_file:
      - .env
    volumes:
      - /var/job/data/db.sqlite:/app/db.sqlite
    restart: on-failure:2
    deploy:
      resources:
        limits:
          cpus: "1"
          memory: 1G
